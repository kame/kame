--- scp/Makefile.orig	Tue Oct 26 03:31:00 1999
+++ scp/Makefile	Tue Nov  9 06:39:11 1999
@@ -2,13 +2,7 @@
 
 PROG=	scp
 BINOWN=	root
-
-.if (${MACHINE_ARCH} == "alpha" || ${MACHINE_ARCH} == "powerpc" || \
-	${MACHINE_ARCH} == "hppa")
-BINMODE=0000
-.else
-BINMODE?=555
-.endif
+BINMODE=555
 
 BINDIR=	/usr/bin
 MAN=	scp.1
